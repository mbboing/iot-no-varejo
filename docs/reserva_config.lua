local config = {};

config.bluetooth_period = 15;
config.upload_period = 4;--A unidade é o bluetooth_period
config.update_period = 2;--De quanto em quanto tempo verifica se há atualização, em minutos

return config;
